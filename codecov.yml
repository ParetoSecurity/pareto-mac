coverage:
  range: 20..60
  status:
    # do not run coverage on patch nor changes
    patch:
      default:
        enabled: false
    project:
      default:
        target: 34%
        informational: false
        only_pulls: false
        if_ci_failed: error
